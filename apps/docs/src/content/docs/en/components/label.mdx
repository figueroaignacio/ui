---
title: Label
description: A label component with required indicator, optional text, and description support.
published: true
date: 2026-03-20
label: New
locale: en
toc:
  visible: true
---

<ComponentPreview component="label" demo="default" />

## Installation

<Steps>

#### Copy and paste the following code into your project.

<ComponentSource component="label" />

#### Update the import paths to match your project structure.

</Steps>

## Anatomy

```tsx
import { Label } from '@/components/ui/label';
```

```tsx
<Label htmlFor="email">Email</Label>
```

## Variants

### Default

A label paired with an input.

<ComponentPreview component="label" demo="default" />

### Required & Optional

Use `required` to show a red asterisk, `optional` to show "(optional)" text, and `description` for helper text.

<ComponentPreview component="label" demo="required" />

## API Reference

### Label

| Prop          | Type              | Default | Description                                       |
| ------------- | ----------------- | ------- | ------------------------------------------------- |
| `required`    | `boolean`         | —       | Shows a red `*` indicator next to the label text. |
| `optional`    | `boolean`         | —       | Shows "(optional)" text next to the label.        |
| `description` | `string`          | —       | Helper text displayed below the label.            |
| `htmlFor`     | `string`          | —       | Associates the label with a form element.         |
| `className`   | `string`          | —       | Additional CSS class names.                       |
| `children`    | `React.ReactNode` | —       | The label content.                                |
